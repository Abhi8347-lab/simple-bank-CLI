# simple-bank-CLI
A command-line banking simulator built in Python that lets a user deposit money, withdraw money, and check their account balance through a simple menu-driven interface. It runs in a continuous loop until manually stopped, and includes basic validation to prevent withdrawals that exceed the available balance.
